Swift context:
- Protocol-Oriented Programming: protocols + extensions over class inheritance
- Value Types: struct and enum (by value) preferred over class (by reference)
- Optionals: explicit nil handling (String?), unwrap via if let / guard let
- Watch for: retain cycles (use weak/unowned), force unwrapping (!), Massive View Controller
- Swift Concurrency: async/await, Actors for thread safety
- Frameworks: SwiftUI (declarative UI), UIKit (imperative), Vapor (server-side)
- Pattern matching: switch with associated values, where clauses
